---
# ansible/roles/layer/arduino-cli/defaults/main.yml
# Opt-in gate — this layer never runs under `apply all` / `upgrade` unless
# flipped. layerctl flips it automatically for `apply arduino-cli` via
# LAYER_EXTRA_VARS (see RaBbLE-OS-layerctl.sh).
rabble_enable_arduino_cli: false
