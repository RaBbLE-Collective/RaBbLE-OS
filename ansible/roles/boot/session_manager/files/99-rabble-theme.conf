# /etc/sddm.conf.d/99-rabble-theme.conf — deployed by Ansible (boot/session_manager)
[Theme]
Current=rabble-aether

[General]
# No qtvirtualkeyboard on RaBbLE-OS — keep the greeter clean
InputMethod=
