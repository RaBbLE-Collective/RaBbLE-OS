---
# boot/plymouth/tasks/packages.yml
- name: Install Plymouth boot animation packages
  ansible.builtin.dnf:
    name:
      - plymouth
      - plymouth-plugin-script
    state: present
  become: true
  tags: [boot, plymouth, packages]
